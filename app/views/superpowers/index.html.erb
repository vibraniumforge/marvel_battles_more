<h1>List of all Superpowers</h1>
<br>
<table>
  <thead>
    <tr>
      <th>Superpower</th>
      <th>Character</th>
      <th>Alias</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
      <td><% @character.superpowers.each do |p|  %>
        <%= link_to p.name, character_path(@character) %>
        <% end %>
      </td>
      <td><%= link_to @character.name, character_path(@character) %></td>
      <td><%= link_to @character.alias, character_path(@character) %></td>
    <tr>
  </tbody>
</table>

<%= link_to "Add a new Superpower", new_character_superpower_path(@character) %>